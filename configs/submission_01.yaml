paths:
  data_dir: data
  artifact_dir: artifacts
  index_path: artifacts/indexes/entities_fts.sqlite
index:
  batch_size: 5000
retrieval:
  operator: OR
run:
  name: submission_01
retrieval:
  mode: concat
  operator: OR
  top_k: 100
  weights:
    content: 1.0
